{
  "type": "function",
  "function": {
    "name": "is_valid_time",
    "description": "Check if a time string is valid in a given format.",
    "long_description": "Validates whether a time string matches a specified format and returns parsed time components if valid.",
    "domain": "Time Operations",
    "parameters": {
      "type": "object",
      "properties": {
        "time_string": {
          "type": "string",
          "description": "Time string to validate"
        },
        "time_format": {
          "type": "string",
          "description": "Time format to validate against (default: %H:%M:%S)"
        }
      },
      "required": [
        "time_string"
      ]
    },
    "example_user_queries": [
      "Call is_valid_time",
      "Execute is_valid_time function",
      "How to check if a time string is valid in a given format?",
      "How to use is_valid_time?",
      "Invoke is_valid_time.",
      "Return result from is_valid_time.",
      "Run is_valid_time with valid parameters.",
      "Show an example for is_valid_time.",
      "Trigger is_valid_time tool.",
      "Use is_valid_time now."
    ]
  }
}
