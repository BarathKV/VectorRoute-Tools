{
  "type": "function",
  "function": {
    "name": "add_hours",
    "description": "Add hours to a given time.",
    "long_description": "Calculates a new time by adding a specified number of hours to a given time.",
    "domain": "Time Operations",
    "parameters": {
      "type": "object",
      "properties": {
        "time_string": {
          "type": "string",
          "description": "Time as string (default format: %H:%M:%S)"
        },
        "hours": {
          "type": "integer",
          "description": "Number of hours to add"
        },
        "time_format": {
          "type": "string",
          "description": "Time format (default: %H:%M:%S)"
        }
      },
      "required": [
        "time_string",
        "hours"
      ]
    },
    "example_user_queries": [
      "Call add_hours",
      "Execute add_hours function",
      "How to add hours to a given time?",
      "How to use add_hours?",
      "Invoke add_hours.",
      "Return result from add_hours.",
      "Run add_hours with valid parameters.",
      "Show an example for add_hours.",
      "Trigger add_hours tool.",
      "Use add_hours now."
    ]
  }
}
