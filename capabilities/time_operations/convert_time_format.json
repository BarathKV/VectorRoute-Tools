{
  "type": "function",
  "function": {
    "name": "convert_time_format",
    "description": "Convert a time string from one format to another.",
    "long_description": "Parses a time string in one format and reformats it to a different time format.",
    "domain": "Time Operations",
    "parameters": {
      "type": "object",
      "properties": {
        "time_string": {
          "type": "string",
          "description": "Time as string"
        },
        "input_format": {
          "type": "string",
          "description": "Input time format (default: %H:%M:%S)"
        },
        "output_format": {
          "type": "string",
          "description": "Output time format (default: %I:%M:%S %p)"
        }
      },
      "required": [
        "time_string"
      ]
    },
    "example_user_queries": [
      "Call convert_time_format",
      "Execute convert_time_format function",
      "How to convert a time string from one format to another?",
      "How to use convert_time_format?",
      "Invoke convert_time_format.",
      "Return result from convert_time_format.",
      "Run convert_time_format with valid parameters.",
      "Show an example for convert_time_format.",
      "Trigger convert_time_format tool.",
      "Use convert_time_format now."
    ]
  }
}
