{
  "type": "function",
  "function": {
    "name": "get_absolute_path",
    "description": "Get the absolute path of a file or directory.",
    "long_description": "Converts a relative path to an absolute path and checks if it exists.",
    "domain": "File Metadata",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Relative or absolute path"
        }
      },
      "required": [
        "path"
      ]
    },
    "example_user_queries": [
      "Call get_absolute_path",
      "Execute get_absolute_path function",
      "How to get the absolute path of a file or directory?",
      "How to use get_absolute_path?",
      "Invoke get_absolute_path.",
      "Return result from get_absolute_path.",
      "Run get_absolute_path with valid parameters.",
      "Show an example for get_absolute_path.",
      "Trigger get_absolute_path tool.",
      "Use get_absolute_path now."
    ]
  }
}
