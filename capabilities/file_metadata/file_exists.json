{
  "type": "function",
  "function": {
    "name": "file_exists",
    "description": "Check if a file or directory exists.",
    "long_description": "Verifies whether a path exists and determines if it is a file or directory.",
    "domain": "File Metadata",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Path to check"
        }
      },
      "required": ["path"]
    },
    "example_user_queries": [
      "Does this file exist?",
      "Check if file exists",
      "Does the path exist?"
    ]
  }
}
