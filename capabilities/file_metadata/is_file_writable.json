{
  "type": "function",
  "function": {
    "name": "is_file_writable",
    "description": "Check if a file is writable.",
    "long_description": "Determines whether the current user has write permission for a file.",
    "domain": "File Metadata",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Path to the file"
        }
      },
      "required": [
        "path"
      ]
    },
    "example_user_queries": [
      "Call is_file_writable",
      "Execute is_file_writable function",
      "How to check if a file is writable?",
      "How to use is_file_writable?",
      "Invoke is_file_writable.",
      "Return result from is_file_writable.",
      "Run is_file_writable with valid parameters.",
      "Show an example for is_file_writable.",
      "Trigger is_file_writable tool.",
      "Use is_file_writable now."
    ]
  }
}
