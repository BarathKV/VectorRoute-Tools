{
  "type": "function",
  "function": {
    "name": "reverse_geocode",
    "description": "Convert coordinates into a readable address.",
    "long_description": "Transforms geographic coordinates (latitude and longitude) into a human-readable address using an external reverse geocoding service.",
    "domain": "Maps & Location",
    "parameters": {
      "type": "object",
      "properties": {
        "latitude": {
          "type": "number",
          "description": "Latitude of the location"
        },
        "longitude": {
          "type": "number",
          "description": "Longitude of the location"
        }
      },
      "required": [
        "latitude",
        "longitude"
      ]
    },
    "example_user_queries": [
      "Call reverse_geocode",
      "Execute reverse_geocode function",
      "How to convert coordinates into a readable address?",
      "How to use reverse_geocode?",
      "Invoke reverse_geocode.",
      "Return result from reverse_geocode.",
      "Run reverse_geocode with valid parameters.",
      "Show an example for reverse_geocode.",
      "Trigger reverse_geocode tool.",
      "Use reverse_geocode now."
    ]
  }
}
