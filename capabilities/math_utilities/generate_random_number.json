{
  "type": "function",
  "function": {
    "name": "generate_random_number",
    "description": "Generate a random integer between min and max (inclusive).",
    "long_description": "Generates a random integer within the specified range, including both the minimum and maximum values.",
    "domain": "Math Utilities",
    "parameters": {
      "type": "object",
      "properties": {
        "min_val": {
          "type": "integer",
          "description": "Minimum value (inclusive)"
        },
        "max_val": {
          "type": "integer",
          "description": "Maximum value (inclusive)"
        }
      },
      "required": [
        "min_val",
        "max_val"
      ]
    },
    "example_user_queries": [
      "Call generate_random_number",
      "Execute generate_random_number function",
      "How to generate a random integer between min and max (inclusive)?",
      "How to use generate_random_number?",
      "Invoke generate_random_number.",
      "Return result from generate_random_number.",
      "Run generate_random_number with valid parameters.",
      "Show an example for generate_random_number.",
      "Trigger generate_random_number tool.",
      "Use generate_random_number now."
    ]
  }
}
